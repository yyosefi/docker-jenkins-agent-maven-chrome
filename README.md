# Jenkins Agent with maven and chrome

The docker is based on Jenkins Agent with OpenJDK 17, maven 3.9.4, chrome and ChromeDriver 127.0.6533.88

## Build docker image
```
docker build --progress plain .
